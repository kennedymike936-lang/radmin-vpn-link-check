# Changelog

## [0.2.0] - 2026-09

诊断可靠性(先修核心):

- 修复 STUN magic cookie 常量错误(0x21 0x12 0xA4 0x42)与 `$Host` 参数命名冲突
- STUN 改为同一本地 UDP 端点探测多个服务器, 比较映射结果
- NAT 判定只报告实测行为: 证据不足时明确输出"未确定", 不再猜测
- 区分"高延迟"与"疑似中继": 高延迟 + 境外连接证据 → 疑似中继; 无证据 → 原因未确认
- IPv6 拆分为三项: 本机地址 / 外网连通 / 与朋友连通(新增 -PeerIPv6)
- 探测失败分类: 超时 / 不可达 / 响应数据异常 / 程序错误, 不再统一归因"网络受限"

报告体验:

- 新报告格式: 摘要优先(现在怎样 / 依据 / 下一步), 明细在后
- 推荐建议附依据; 第三方方案说明标注核实日期
- 自动保存 TXT(阅读) + JSON(分析) + 脱敏分享 JSON(默认隐藏完整IP/设备名)

新增功能:

- `-Compare` 两端报告对照(版本/双向延迟/丢包/IPv6/NAT/运营商/中继判定)
- `-Fix` 修复模式: 先预览再执行, 只改蓝盾虚拟网卡类型, 规则带 `NetCheck-Radmin-` 前缀
- `-Undo` 撤销: 依据状态文件仅撤销本工具的改动, 逐项复核
- 失败如实显示, 不再出现"命令失败但界面显示修复完成"

工程化:

- 新增 tests/NetCheck.Tests.ps1 纯函数单元测试(STUN解析/判定逻辑/脱敏)
- LICENSE (MIT)、CHANGELOG、FEEDBACK(反馈模板+案例记录)、README_EN

## [0.1.0] - 2026-09

- 初始版: 蓝盾状态/同伴发现/延迟实测/STUN探测/IPv6检测/防火墙检查/方案推荐
